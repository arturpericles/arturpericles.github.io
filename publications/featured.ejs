
<%
  const P = templateParams || {};
  const cols = P.cols || 3;
%>
```{=html}

<div class="list grid quarto-listing-cols-3">
  <% for (const item of items) { %>
    <div class="g-col-1" <%- metadataAttrs(item) %>>
      <a class="quarto-grid-link" href="<%- item.path %>">
        <div class="quarto-grid-item card h-100 card-left">
          <div class="card-body post-contents">

            <% if (item.title) { %>
            <h5 class="no-anchor card-title listing-title"><%- item["title-short"] || item.title %></h5>
            <% } %>

            <% if (item.citation['container-title']) { %>
            <div class="metadata-value"><%= item.citation['container-title'] || item.citation['container-title-short'] %></div>
            <% } %>

            <% if (item.date) { %>
            <div class="card-attribution card-text-small"><%= item.date %></div>
            <% } %>
            <% if (item.citation.status) { %>
            <div class="card-text-small text-muted">(<%= item.citation.status %>)</div>
            <% } %>

          </div>
        </div>
      </a>
    </div>
  <% } %>

```
